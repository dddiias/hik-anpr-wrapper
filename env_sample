ENABLE_SNOW_WORKER=true                   # Включить фонового снегового воркера
SNOW_VIDEO_SOURCE_URL=""                  # RTSP снеговой камеры
SNOW_CAMERA_ID=""                         # ID снеговой камеры (попадает в событие снега)
SNOW_YOLO_MODEL_PATH="yolov8n.pt"         # Вес для детектора грузовиков
SNAPSHOT_BASE_DIR="snapshots"             # Не используется сейчас (сохранение в память)
SNOW_CENTER_ZONE_START_X=0.35             # Левая граница центральной зоны (доля ширины)
SNOW_CENTER_ZONE_END_X=0.65               # Правая граница центральной зоны (доля ширины)
SNOW_MIN_DIRECTION_DELTA=5                # Минимальный сдвиг по X для подтверждения движения слева направо (px)
GEMINI_API_KEY=""                         # Ключ Gemini (для анализа снега при мердже)
GEMINI_MODEL="gemini-2.5-flash"           # Модель Gemini
MERGE_WINDOW_SECONDS=5                    # Окно (сек), в пределах которого снег считается предшествующим номеру
MERGE_TTL_SECONDS=10                      # TTL (сек) хранения снеговых событий в очереди
SNOW_SHOW_WINDOW=                         # true/false — показывать окно предпросмотра снеговой камеры
UPSTREAM_URL="backend service"            # URL, куда отправляем объединённые события
PLATE_CAMERA_ID=""                        # ID номерной камеры (camera_id в event)
